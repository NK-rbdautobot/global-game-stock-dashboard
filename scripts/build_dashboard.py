#!/usr/bin/env python3
from __future__ import annotations
from pathlib import Path
ROOT = Path(__file__).resolve().parents[1]
DOCS = ROOT / "docs"
HTML = r'''<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>글로벌 경쟁사 피어 대시보드</title><script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script><style>
:root{--bg:#0b1020;--panel:#121a2f;--panel2:#17213a;--soft:#0f172a;--text:#eef2ff;--muted:#9aa7c7;--line:#25304d;--accent:#7dd3fc;--nexon:#8b5cf6;--good:#34d399;--bad:#fb7185;--warn:#fbbf24;--shadow:0 18px 40px rgba(0,0,0,.24)}body.light{--bg:#f5f7fb;--panel:#fff;--panel2:#edf2f7;--soft:#f8fafc;--text:#111827;--muted:#64748b;--line:#d5deee;--accent:#0369a1;--nexon:#6d28d9;--good:#059669;--bad:#e11d48;--warn:#b45309;--shadow:0 14px 28px rgba(15,23,42,.08)}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top left,rgba(125,211,252,.14),transparent 30%),var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.wrap{max-width:1520px;margin:auto;padding:24px 18px 70px}.top{position:sticky;top:0;z-index:50;background:linear-gradient(180deg,var(--bg) 82%,transparent);backdrop-filter:blur(12px);padding:12px 0 14px}.topline{display:flex;justify-content:space-between;gap:16px;align-items:center}.brand h1{font-size:28px;margin:0}.meta{color:var(--muted);font-size:13px;margin-top:7px}.actions,.tabs,.inline-actions{display:flex;gap:8px;flex-wrap:wrap}.tabs{margin-top:13px}.btn,button{border:1px solid var(--line);background:var(--panel);color:var(--text);border-radius:12px;padding:10px 13px;cursor:pointer;text-decoration:none;font-weight:800}.btn:hover,button:hover{border-color:var(--accent)}.tab.active{background:linear-gradient(135deg,var(--accent),var(--nexon));color:white;border-color:transparent}.section{display:none}.section.active{display:block}.cards{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:13px;margin:18px 0}.card{background:linear-gradient(180deg,var(--panel),var(--panel2));border:1px solid var(--line);border-radius:18px;padding:15px;box-shadow:var(--shadow);min-width:0}.card.nexon{border:2px solid var(--nexon);box-shadow:0 0 0 4px rgba(139,92,246,.12),var(--shadow)}.k{color:var(--muted);font-size:12px}.v{font-size:24px;font-weight:900;margin-top:7px;letter-spacing:-.02em}.sub{color:var(--muted);font-size:12px;margin-top:6px;line-height:1.45}.grid{display:grid;grid-template-columns:1.15fr .85fr;gap:14px}.grid2{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:14px}.chartbox{height:360px;overflow:hidden}.detail-chart{height:520px;overflow:visible}.detail-chart canvas{height:420px!important;max-height:420px!important}.compare-chart{height:560px;overflow:visible}.compare-chart canvas{height:450px!important;max-height:450px!important}.toolbar,.detail-toolbar{position:sticky;top:94px;z-index:45;display:grid;grid-template-columns:1.4fr repeat(3,minmax(145px,.45fr));gap:9px;margin:16px 0;padding:10px;background:rgba(15,23,42,.78);border:1px solid var(--line);border-radius:16px;backdrop-filter:blur(12px)}body.light .toolbar,body.light .detail-toolbar{background:rgba(248,250,252,.86)}input,select{width:100%;background:var(--panel);color:var(--text);border:1px solid var(--line);border-radius:12px;padding:11px}.table-wrap{overflow:visible;border-radius:18px;border:1px solid var(--line);background:var(--panel)}table{width:100%;border-collapse:separate;border-spacing:0;min-width:1120px}th,td{padding:12px 10px;border-bottom:1px solid var(--line);text-align:right;font-size:13px;vertical-align:middle}th{position:sticky;top:156px;z-index:40;background:#e9eef5;color:#63738b;font-weight:900;font-size:16px}body:not(.light) th{background:#111b30;color:#b6c3dd}td:first-child,th:first-child{text-align:left}.name{font-weight:900}.small{display:block;color:var(--muted);font-size:12px;margin-top:3px}.up{color:var(--good);font-weight:900}.down{color:var(--bad);font-weight:900}.pill{display:inline-flex;border:1px solid var(--line);border-radius:999px;padding:4px 9px;font-size:12px;color:var(--muted);background:rgba(125,211,252,.07)}tr.clickable{cursor:pointer}tr.clickable:hover td{background:rgba(125,211,252,.08)}tr.nexon-row td{background:rgba(139,92,246,.14);border-top:1px solid rgba(139,92,246,.55);border-bottom:1px solid rgba(139,92,246,.55)}tr.nexon-row td:first-child{border-left:4px solid var(--nexon)}.news-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.news{padding:16px;border:1px solid var(--line);border-radius:16px;background:var(--soft)}.news a{color:var(--accent);font-weight:900;text-decoration:none}.news-title{font-size:20px;line-height:1.35;margin:8px 0}.news-body{margin:14px 0 0;color:var(--text);line-height:1.85;font-size:15px}.dash-line{margin:0 0 10px 0}.news-body .dash-line:last-child,.news-comment .dash-line:last-child{margin-bottom:0}.news-comment{margin-top:0;padding:0;background:transparent;color:var(--text);font-weight:400;line-height:1.85;font-size:15px}.news-comment .dash-line{color:var(--text);font-weight:400}.news-comment-label{font-weight:900;color:var(--bad);margin:0}.detail-head{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;margin:18px 0}.detail-title h2{margin:0;font-size:30px}.company-picker{max-width:360px}.compare-picker{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:12px 0}.pager{display:flex;gap:8px;align-items:center;justify-content:center;margin-top:14px}.selected-tags{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.tag{display:inline-flex;align-items:center;gap:8px;border:1px solid var(--accent);border-radius:999px;padding:7px 10px;background:rgba(125,211,252,.1);font-weight:800}.tag button{padding:2px 7px;border-radius:999px}.foot{color:var(--muted);font-size:12px;line-height:1.6;margin-top:16px}@media(max-width:1100px){.cards{grid-template-columns:repeat(2,1fr)}.grid,.grid2{grid-template-columns:1fr}.toolbar,.detail-toolbar{grid-template-columns:1fr 1fr;top:120px}.compare-picker{grid-template-columns:1fr}.news-list{grid-template-columns:1fr}.topline,.detail-head{display:block}.actions{margin-top:12px}th{top:205px}}@media(max-width:640px){.cards,.toolbar,.detail-toolbar{grid-template-columns:1fr}.wrap{padding:12px 8px}.brand h1{font-size:22px}th,td{font-size:12px;padding:9px 7px}.v{font-size:20px}}
</style></head><body><div class="wrap"><header class="top"><div class="topline"><div class="brand"><h1>글로벌 경쟁사 피어 대시보드</h1><div class="meta" id="topMeta">주가·시총·기업별 재무성과·번역 뉴스 상세 페이지</div></div><div class="actions"><button id="themeBtn">라이트/다크</button><button id="refreshBtn">데이터 새로고침</button><a class="btn" href="data/market_quotes.csv" download>CSV 다운로드</a></div></div><nav class="tabs"><button class="tab active" data-page="overview">종합 상황판</button><button class="tab" data-page="companyDetail">회사 상세</button><button class="tab" data-page="compare">회사 비교</button></nav></header>
<section id="overview" class="section active"><div class="cards" id="cards"></div><div class="grid"><div class="card chartbox"><canvas id="capChart"></canvas></div><div class="card chartbox"><canvas id="countryChart"></canvas></div></div><div class="toolbar"><input id="search" placeholder="회사명/티커/국가/거래소 검색"><select id="country"><option value="">전체 국가</option></select><select id="category"><option value="">전체 유형</option></select><select id="sort"><option value="market_cap_krw_desc">시총 KRW 높은순</option><option value="change_percent_desc">상승률 높은순</option><option value="change_percent_asc">하락률 높은순</option><option value="company_ko_asc">회사명 가나다순</option></select></div><div class="table-wrap"><table id="tbl"><thead><tr><th>회사</th><th>유형</th><th>국가/거래소</th><th>티커</th><th>현재가</th><th>등락률</th><th>시총 현지통화</th><th>시총 KRW</th><th>거래량</th><th>업데이트</th><th>상태</th></tr></thead><tbody></tbody></table></div></section>
<section id="peer" class="section"><div class="grid2"><div class="card chartbox"><canvas id="platformChart"></canvas></div><div class="card chartbox"><canvas id="gameChart"></canvas></div></div><div class="cards" id="peerCards"></div></section>
<section id="companyDetail" class="section"><div class="card"><div class="k">회사 선택</div><select id="detailSelect" class="company-picker"></select></div><div id="detail"></div></section>
<section id="compare" class="section"><div class="card"><h2>회사 비교</h2><p class="sub">최대 5개 회사를 태그처럼 추가해 분기/연간 매출과 영업이익을 겹쳐 비교합니다. 넥슨은 재무성과 BD 정리 전까지 선택 목록에서 제외됩니다.</p><div class="detail-toolbar" style="position:static;grid-template-columns:1fr auto"><select id="compareAdd"><option value="">비교할 회사 추가</option></select><button id="compareClear">초기화</button></div><div class="selected-tags" id="compareTags"></div></div><div class="grid2"><div class="card compare-chart"><h3>분기 매출 비교</h3><canvas id="cmpQuarterRev"></canvas></div><div class="card compare-chart"><h3>분기 영업이익 비교</h3><canvas id="cmpQuarterOp"></canvas></div><div class="card compare-chart"><h3>연간 매출 비교</h3><canvas id="cmpYearRev"></canvas></div><div class="card compare-chart"><h3>연간 영업이익 비교</h3><canvas id="cmpYearOp"></canvas></div></div></section><div class="foot" id="foot"></div></div>
<script>
let RAW=[],META={},ARCH={financial_archive:{rows:[]},news_archive:{rows:[]}},charts={},SELECTED='',NEWS_PAGE=1,COMPARE_SELECTED=[];const KRW_EOK=100000000,KRW_JO=1000000000000,PAGE_SIZE=6,PALETTE=['#7dd3fc','#34d399','#fbbf24','#fb7185','#a78bfa'];
function n(v){return Number(v||0)}function fmtNum(v,d=0){return(v==null||!isFinite(Number(v)))?'-':Number(v).toLocaleString('ko-KR',{maximumFractionDigits:d,minimumFractionDigits:d})}function fmtKrw(v){v=n(v);if(!v)return'-';if(Math.abs(v)>=KRW_JO){let jo=Math.floor(v/KRW_JO),eok=Math.round((v-jo*KRW_JO)/KRW_EOK);return eok?`${fmtNum(jo)}조 ${fmtNum(eok)}억 원`:`${fmtNum(jo)}조 원`;}return`${fmtNum(v/KRW_EOK,0)}억 원`}function fmtCapLocal(v,cur){if(!v)return'-';const unit=(cur==='KRW'||cur==='JPY')?KRW_EOK:1000000,label=(cur==='KRW'||cur==='JPY')?'억':'M';return`${fmtNum(v/unit,1)} ${label} ${cur}`}function fmtPrice(v,cur){return v==null?'-':`${fmtNum(v,(cur==='JPY'||cur==='KRW')?0:2)} ${cur||''}`}function pct(v){if(v==null||!isFinite(Number(v)))return'-';return`<span class="${v>=0?'up':'down'}">${v>=0?'+':''}${fmtNum(v,2)}%</span>`}function dt(s){try{return new Date(s).toLocaleString('ko-KR',{timeZone:'Asia/Seoul'})}catch(e){return s||'-'}}function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function dateKey(s){let m=String(s||'').match(/(20\d{2})[-.\/]\s*(\d{1,2})[-.\/]\s*(\d{1,2})/);return m?`${m[1]}-${String(+m[2]).padStart(2,'0')}-${String(+m[3]).padStart(2,'0')}`:''}
function showPage(id){document.querySelectorAll('.tab,.section').forEach(x=>x.classList.remove('active'));document.querySelector(`.tab[data-page="${id}"]`)?.classList.add('active');document.getElementById(id).classList.add('active');setTimeout(refreshCharts,50)}
function filtered(){const q=search.value.trim().toLowerCase(),c=country.value,cat=category.value,sort=document.getElementById('sort').value;let rows=RAW.filter(r=>(!c||r.country===c)&&(!cat||r.category===cat)&&(!q||[r.company_ko,r.company_en,r.yahoo_symbol,r.input_ticker,r.country,r.exchange,r.category].join(' ').toLowerCase().includes(q)));rows.sort((a,b)=>sort==='market_cap_krw_desc'?n(b.market_cap_krw)-n(a.market_cap_krw):sort==='change_percent_desc'?n(b.change_percent)-n(a.change_percent):sort==='change_percent_asc'?n(a.change_percent)-n(b.change_percent):String(a.company_ko).localeCompare(String(b.company_ko),'ko'));return rows}function rankInGroup(row){let peers=RAW.filter(r=>r.category===row?.category&&r.market_cap_krw).sort((a,b)=>n(b.market_cap_krw)-n(a.market_cap_krw));return peers.findIndex(r=>r.company_ko===row?.company_ko)+1}
function renderCards(rows){const ok=rows.filter(r=>r.data_status==='ok'),total=ok.reduce((s,r)=>s+n(r.market_cap_krw),0),nexon=RAW.find(r=>r.company_ko==='넥슨'),game=RAW.filter(r=>r.category==='게임사'),platform=RAW.filter(r=>r.category==='플랫폼사');cards.innerHTML=`<div class="card nexon"><div class="k">NEXON 하이라이트</div><div class="v">${fmtKrw(nexon?.market_cap_krw)}</div><div class="sub">게임사 내 시총 ${rankInGroup(nexon)}위 · ${pct(nexon?.change_percent)}</div></div><div class="card"><div class="k">표시 기업</div><div class="v">${rows.length}개</div><div class="sub">디폴트는 게임사 유형</div></div><div class="card"><div class="k">표시군 합산 시총</div><div class="v">${fmtKrw(total)}</div><div class="sub">현재 필터 기준</div></div><div class="card"><div class="k">플랫폼사 합산</div><div class="v">${fmtKrw(platform.reduce((s,r)=>s+n(r.market_cap_krw),0))}</div><div class="sub">Alphabet · Apple · Microsoft</div></div><div class="card"><div class="k">게임사 합산</div><div class="v">${fmtKrw(game.reduce((s,r)=>s+n(r.market_cap_krw),0))}</div><div class="sub">플랫폼 3사 제외</div></div>`}
function renderTable(rows){document.querySelector('#tbl tbody').innerHTML=rows.map(r=>{let clickable=r.company_ko!=='넥슨';return`<tr class="${clickable?'clickable':''} ${r.company_ko==='넥슨'?'nexon-row':''}" data-company="${esc(r.company_ko)}"><td><span class="name">${esc(r.company_ko)}${r.company_ko==='넥슨'?' ★':''}</span><span class="small">${esc(r.company_en)}${clickable?' · 클릭해서 상세 보기':' · 상세 제외'}</span></td><td><span class="pill">${r.category}</span></td><td>${r.country}<span class="small">${r.exchange}</span></td><td>${r.input_ticker}<span class="small">${r.yahoo_symbol}</span></td><td>${fmtPrice(r.price,r.currency)}</td><td>${pct(r.change_percent)}</td><td>${fmtCapLocal(r.market_cap_local,r.currency)}</td><td><b>${fmtKrw(r.market_cap_krw)}</b></td><td>${fmtNum(r.volume,0)}</td><td>${dt(r.market_time||r.as_of_utc)}</td><td>${r.data_status}</td></tr>`}).join('');document.querySelectorAll('#tbl tbody tr.clickable').forEach(tr=>tr.onclick=()=>openCompany(tr.dataset.company))}
function chartOpts(type){return{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:getComputedStyle(document.body).getPropertyValue('--text')}}},scales:type==='doughnut'?{}:{x:{ticks:{color:getComputedStyle(document.body).getPropertyValue('--muted'),maxRotation:0,autoSkip:true}},y:{beginAtZero:true,ticks:{color:getComputedStyle(document.body).getPropertyValue('--muted')}}}}}function makeChart(id,type,data,opt={}){if(charts[id])charts[id].destroy();let el=document.getElementById(id);if(!el)return;charts[id]=new Chart(el,{type,data,options:Object.assign(chartOpts(type),opt)})}
function renderOverviewCharts(rows){let ok=rows.filter(r=>r.market_cap_krw).sort((a,b)=>n(b.market_cap_krw)-n(a.market_cap_krw)).slice(0,20);makeChart('capChart','bar',{labels:ok.map(r=>r.company_ko),datasets:[{label:'시총 KRW(조)',data:ok.map(r=>+(n(r.market_cap_krw)/KRW_JO).toFixed(1)),backgroundColor:ok.map(r=>r.company_ko==='넥슨'?'#8b5cf6':r.category==='플랫폼사'?'#38bdf8':'#34d399')}]},{indexAxis:'y'});let by={};rows.filter(r=>r.market_cap_krw).forEach(r=>by[r.country]=(by[r.country]||0)+n(r.market_cap_krw));let ents=Object.entries(by).sort((a,b)=>b[1]-a[1]);makeChart('countryChart','doughnut',{labels:ents.map(e=>e[0]),datasets:[{data:ents.map(e=>+(e[1]/KRW_JO).toFixed(1)),backgroundColor:['#7dd3fc','#34d399','#fbbf24','#fb7185','#a78bfa','#f472b6','#60a5fa','#94a3b8']}]} )}
function renderPeer(){['플랫폼사','게임사'].forEach((cat,i)=>{let id=i?'gameChart':'platformChart', rows=RAW.filter(r=>r.category===cat&&r.market_cap_krw).sort((a,b)=>n(b.market_cap_krw)-n(a.market_cap_krw)).slice(0,i?20:3);makeChart(id,'bar',{labels:rows.map(r=>r.company_ko),datasets:[{label:`${cat} 시총 KRW(조)`,data:rows.map(r=>+(n(r.market_cap_krw)/KRW_JO).toFixed(1)),backgroundColor:rows.map(r=>r.company_ko==='넥슨'?'#8b5cf6':i?'#34d399':'#38bdf8')}]},{indexAxis:'y'});});let nexon=RAW.find(r=>r.company_ko==='넥슨');peerCards.innerHTML=`<div class="card nexon"><div class="k">넥슨 게임사 순위</div><div class="v">${rankInGroup(nexon)}위</div><div class="sub">플랫폼 3사 제외 게임사끼리 비교</div></div>`+['플랫폼사','게임사'].map(cat=>{let rs=RAW.filter(r=>r.category===cat),up=rs.filter(r=>n(r.change_percent)>0).length;return`<div class="card"><div class="k">${cat}</div><div class="v">${rs.length}개</div><div class="sub">상승 ${up}개 · 합산 ${fmtKrw(rs.reduce((s,r)=>s+n(r.market_cap_krw),0))}</div></div>`}).join('')}
function finFor(c){return (ARCH.financial_archive?.rows||[]).find(x=>x.company_ko===c)}function allFinCompanies(){return (ARCH.financial_archive?.rows||[]).map(x=>x.company_ko).sort((a,b)=>a.localeCompare(b,'ko'))}function newsFor(c){return (ARCH.news_archive?.rows||[]).filter(x=>x.company_ko===c)}function openCompany(company){if(company==='넥슨')return;SELECTED=company;NEWS_PAGE=1;detailSelect.value=company;renderDetail();showPage('companyDetail');window.scrollTo(0,0)}function rowMetric(label,value){return`<div class="card"><div class="k">${label}</div><div class="v">${value}</div></div>`}
function renderDetail(){let m=RAW.find(r=>r.company_ko===SELECTED)||RAW.find(r=>r.company_ko==='텐센트')||RAW[0];if(!m||m.company_ko==='넥슨'){m=RAW.find(r=>r.company_ko==='텐센트')||RAW.find(r=>r.company_ko!=='넥슨')||RAW[0]}SELECTED=m.company_ko;if(detailSelect)detailSelect.value=SELECTED;let fin=finFor(m.company_ko);detail.innerHTML=`<div class="detail-head"><div class="detail-title"><h2>${esc(m.company_ko)}</h2><div class="meta">${esc(m.company_en)} · ${m.input_ticker} · ${m.category}</div></div></div><div class="cards">${rowMetric('현재가',fmtPrice(m.price,m.currency))}${rowMetric('등락률',pct(m.change_percent))}${rowMetric('시가총액 KRW',fmtKrw(m.market_cap_krw))}${rowMetric(`${m.category} 내 시총 순위`,rankInGroup(m)+'위')}${rowMetric('업데이트',dt(m.market_time||m.as_of_utc))}</div><div class="grid2"><div class="card detail-chart"><h3>분기 성과 <span class="small">최근 6분기 · ${esc(fin?.revenue_unit||'매출')} / ${esc(fin?.operating_profit_unit||'영업이익')}</span></h3><canvas id="detailQuarterChart"></canvas></div><div class="card detail-chart"><h3>연간 성과 <span class="small">${esc(fin?.revenue_unit||'매출')} / ${esc(fin?.operating_profit_unit||'영업이익')}</span></h3><canvas id="detailYearChart"></canvas></div></div><div class="card"><h3>관련 기사</h3><div class="detail-toolbar" style="position:static;grid-template-columns:1.2fr .4fr .4fr .3fr"><input id="newsKeyword" placeholder="키워드 검색"><input id="newsFrom" type="date"><input id="newsTo" type="date"><button id="newsReset">초기화</button></div><div class="news-list" id="detailNews"></div><div class="pager" id="newsPager"></div></div>`;renderDetailCharts(fin);['newsKeyword','newsFrom','newsTo'].forEach(id=>document.getElementById(id).addEventListener('input',()=>{NEWS_PAGE=1;renderDetailNews()}));newsReset.onclick=()=>{newsKeyword.value='';newsFrom.value='';newsTo.value='';NEWS_PAGE=1;renderDetailNews()};renderDetailNews()}
function renderDetailCharts(fin){let qs=(fin?.quarters||[]).slice(-6),ys=fin?.years||[],unit=`${fin?.revenue_unit||'매출'} / ${fin?.operating_profit_unit||'영업이익'}`;makeChart('detailQuarterChart','line',{labels:qs.map(x=>x.period),datasets:[{label:'매출',data:qs.map(x=>x.revenue),borderColor:'#7dd3fc',backgroundColor:'rgba(125,211,252,.14)',tension:.25,pointRadius:3},{label:'영업이익',data:qs.map(x=>x.operating_profit),borderColor:'#34d399',backgroundColor:'rgba(52,211,153,.14)',tension:.25,pointRadius:3}]},{plugins:{legend:{position:'top',labels:{boxWidth:28,color:getComputedStyle(document.body).getPropertyValue('--text')}}},scales:{y:{beginAtZero:true,title:{display:true,text:unit,color:getComputedStyle(document.body).getPropertyValue('--muted')}}}});makeChart('detailYearChart','bar',{labels:ys.map(x=>x.year),datasets:[{label:'매출',data:ys.map(x=>x.revenue),backgroundColor:'rgba(125,211,252,.65)'},{label:'영업이익',data:ys.map(x=>x.operating_profit),backgroundColor:'rgba(52,211,153,.65)'}]},{plugins:{legend:{position:'top',labels:{boxWidth:28,color:getComputedStyle(document.body).getPropertyValue('--text')}}},scales:{y:{beginAtZero:true,title:{display:true,text:unit,color:getComputedStyle(document.body).getPropertyValue('--muted')}}}})}
function splitComment(s){let parts=String(s||'').split(/\[Comment\]/i);if(parts.length<=1)return{body:parts[0]||'',comment:''};return{body:parts[0].trim(),comment:(parts[1]||'').trim()}}function dashLines(s){let parts=String(s||'').split(/\s+-\s+/).map(x=>x.trim()).filter(Boolean);if(parts.length<=1)parts=String(s||'').split(/\n\s*-\s*/).map(x=>x.trim()).filter(Boolean);if(parts.length<=1&&String(s||'').trim())parts=[String(s).trim()];return parts.map(p=>`<p class="dash-line">- ${esc(p.replace(/^-\s*/,''))}</p>`).join('')}function newsBullets(s){let obj=splitComment(s);let html=`<div class="news-body">${dashLines(obj.body)}</div>`;if(obj.comment)html+=`<div class="news-comment"><div class="news-comment-label">[Comment]</div>${dashLines(obj.comment)}</div>`;return html}
function filteredNews(){let q=(document.getElementById('newsKeyword')?.value||'').toLowerCase(),from=document.getElementById('newsFrom')?.value||'',to=document.getElementById('newsTo')?.value||'';return newsFor(SELECTED).filter(x=>{let d=dateKey(x.published_at);return(!q||[x.title_ko,x.summary_ko,x.keyword].join(' ').toLowerCase().includes(q))&&(!from||d>=from)&&(!to||d<=to)}).sort((a,b)=>dateKey(b.published_at).localeCompare(dateKey(a.published_at)))}
function renderDetailNews(){let rows=filteredNews(),pages=Math.max(1,Math.ceil(rows.length/PAGE_SIZE));if(NEWS_PAGE>pages)NEWS_PAGE=pages;let pageRows=rows.slice((NEWS_PAGE-1)*PAGE_SIZE,NEWS_PAGE*PAGE_SIZE);detailNews.innerHTML=pageRows.map(x=>`<article class="news"><span class="pill">${esc(x.published_at||'날짜 없음')}</span><h3 class="news-title"><a href="${esc(x.url)}" target="_blank" rel="noopener noreferrer">${esc(x.title_ko)}</a></h3>${newsBullets((x.summary_ko||'')+(x.integrated_note?(String(x.integrated_note).match(/\[Comment\]/i)?` ${x.integrated_note}`:` [Comment] ${x.integrated_note}`):''))}</article>`).join('')||'<p class="sub">조건에 맞는 관련 기사가 없습니다.</p>';newsPager.innerHTML=`<button ${NEWS_PAGE<=1?'disabled':''} onclick="NEWS_PAGE--;renderDetailNews()">이전</button><span class="sub">${NEWS_PAGE} / ${pages} · ${rows.length}건</span><button ${NEWS_PAGE>=pages?'disabled':''} onclick="NEWS_PAGE++;renderDetailNews()">다음</button>`}
function renderComparePicker(){let names=allFinCompanies();compareAdd.innerHTML='<option value="">비교할 회사 추가</option>'+names.filter(c=>!COMPARE_SELECTED.includes(c)).map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('');compareTags.innerHTML=COMPARE_SELECTED.map(c=>`<span class="tag">${esc(c)} <button onclick="removeCompare('${esc(c)}')">×</button></span>`).join('')||'<span class="sub">선택된 회사가 없습니다.</span>'}function addCompare(c){if(!c)return;if(COMPARE_SELECTED.includes(c))return;if(COMPARE_SELECTED.length>=5){alert('최대 5개 회사까지 선택할 수 있습니다.');return}COMPARE_SELECTED.push(c);renderComparePicker();renderCompareCharts()}function removeCompare(c){COMPARE_SELECTED=COMPARE_SELECTED.filter(x=>x!==c);renderComparePicker();renderCompareCharts()}function selectedCompare(){return COMPARE_SELECTED.map(c=>finFor(c)).filter(Boolean)}function labelOrder(label,key){let s=String(label||'');if(key==='years')return Number(s)||0;let m=s.match(/(20\d{2})\s*Q([1-4])/);return m?(Number(m[1])*4+Number(m[2])):0}function unionLabels(rows,key){return [...new Set(rows.flatMap(r=>(r[key]||[]).map(x=>x.period||x.year)).filter(Boolean))].sort((a,b)=>labelOrder(a,key)-labelOrder(b,key)).slice(-10)}function valueFor(arr,label,field){let x=(arr||[]).find(v=>(v.period||v.year)===label);return x?x[field]:null}
function renderCompareCharts(){let rows=selectedCompare();let qLabs=unionLabels(rows,'quarters'),yLabs=unionLabels(rows,'years');function ds(field,key){return rows.map((r,i)=>({label:r.company_ko,data:(key==='quarters'?qLabs:yLabs).map(l=>valueFor(r[key],l,field+'_krw_eok')),borderColor:PALETTE[i%PALETTE.length],backgroundColor:PALETTE[i%PALETTE.length]+'66',tension:.25}))}let axis={scales:{y:{beginAtZero:true,title:{display:true,text:'KRW 환산(억 원)',color:getComputedStyle(document.body).getPropertyValue('--muted')}}}};makeChart('cmpQuarterRev','line',{labels:qLabs,datasets:ds('revenue','quarters')},axis);makeChart('cmpQuarterOp','line',{labels:qLabs,datasets:ds('operating_profit','quarters')},axis);makeChart('cmpYearRev','bar',{labels:yLabs,datasets:ds('revenue','years')},axis);makeChart('cmpYearOp','bar',{labels:yLabs,datasets:ds('operating_profit','years')},axis)}
function refreshCharts(){if(document.getElementById('overview').classList.contains('active'))renderOverviewCharts(filtered());if(document.getElementById('companyDetail').classList.contains('active'))renderDetail();if(document.getElementById('compare').classList.contains('active'))renderCompareCharts()}
function refresh(){let rows=filtered();renderCards(rows);renderTable(rows);renderOverviewCharts(rows);if(!SELECTED)SELECTED='텐센트';renderDetail();renderCompareCharts();topMeta.textContent=`최종 갱신 ${dt(META.generated_at_utc)} · 정상 시총 ${META.ok_count}/${META.company_count} · 기업별 재무 ${ARCH.financial_archive?.count||0}개 · 번역 기사 ${ARCH.news_archive?.count||0}건`;foot.innerHTML=''}
async function load(){META=await(await fetch('data/market_quotes.json?ts='+Date.now())).json();RAW=META.rows||[];ARCH=await(await fetch('data/archives.json?ts='+Date.now())).json();['country','category'].forEach(id=>{let sel=document.getElementById(id);[...new Set(RAW.map(r=>r[id]).filter(Boolean))].sort().forEach(v=>sel.insertAdjacentHTML('beforeend',`<option value="${v}">${v}</option>`))});allFinCompanies().forEach(c=>detailSelect.insertAdjacentHTML('beforeend',`<option value="${c}">${c}</option>`));detailSelect.onchange=()=>{SELECTED=detailSelect.value;NEWS_PAGE=1;renderDetail()};COMPARE_SELECTED=allFinCompanies().filter(c=>['시프트업','엔씨소프트','크래프톤'].includes(c)).slice(0,3);if(COMPARE_SELECTED.length<3)COMPARE_SELECTED=allFinCompanies().slice(0,3);renderComparePicker();compareAdd.onchange=()=>{addCompare(compareAdd.value);compareAdd.value=''};compareClear.onclick=()=>{COMPARE_SELECTED=[];renderComparePicker();renderCompareCharts()};category.value='게임사';SELECTED='텐센트';refresh()}
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>showPage(b.dataset.page));themeBtn.onclick=()=>{document.body.classList.toggle('light');setTimeout(refreshCharts,50)};refreshBtn.onclick=()=>location.reload();['search','country','category','sort'].forEach(id=>document.getElementById(id).addEventListener('input',refresh));load().catch(e=>foot.textContent='데이터 로드 실패: '+e);
</script></body></html>'''
DOCS.mkdir(parents=True, exist_ok=True)
(DOCS / "index.html").write_text(HTML, encoding="utf-8")
(DOCS / ".nojekyll").write_text("", encoding="utf-8")
print(DOCS / "index.html")
